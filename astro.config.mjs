// @ts-check
import { defineConfig } from "astro/config";

// https://astro.build/config
export default defineConfig({
  site: "https://jraugel.github.io",
  // For a user site served at the root (jraugel.github.io) no base is needed.
  // For a project site, set `base: "/<repo>"` instead.
});
